# поднимаем контейнер с сервером nats.io
docker compose up

# в интерактивном режиме запуск контейнера с готовым cli клиентом nats.io, подписка на топик time.tick и ct.state
docker run -it --network rocket_league_default natsio/nats-box
a5c7450a4398:~# nats --server nats://nats:4222 sub time.tick ct.state

# отдельный запуск jvm
gradle :ct-vm:run
gradle :time-advancer:run
